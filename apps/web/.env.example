# `apps/web/.env.development` and `.env.production` set `NEXT_PUBLIC_WORDSET=cleanroom`
# so the app loads `public/cleanroom/twelljr-*.json`. Override in `.env.local` if needed.
# NEXT_PUBLIC_WORDSET=cleanroom
